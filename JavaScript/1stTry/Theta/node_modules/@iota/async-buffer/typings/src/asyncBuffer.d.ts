import * as Promise from 'bluebird';
export interface AsyncBuffer<T> {
    read: () => Promise<T>;
    write: (value: T) => void;
    dump: () => ReadonlyArray<T>;
    inboundLength: () => number;
    outboundLength: () => number;
}
export declare const asyncBuffer: <T>(length?: number) => AsyncBuffer<T>;
